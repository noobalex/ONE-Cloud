server:
  port: 8401

spring:
  application:
    name: ONE-Monitor-Admin
  boot:
    admin:
      ui:
        title: ${spring.application.name}
  security:
    user:
      name: one
      password: 123456
